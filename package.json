{
  "name": "whatsapp-business-campaign-sender",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:api": "npm --workspace @campaign-sender/api run dev",
    "dev:web": "npm --workspace @campaign-sender/web run dev",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
